{
  "name": "graph",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only index.ts",
    "test": "ts-node-dev --respawn --transpile-only test.ts" 
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "tsc": "^2.0.4",
    "typescript": "^4.8.2"
  }
}
